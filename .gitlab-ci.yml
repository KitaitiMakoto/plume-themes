image: kitaitimakoto/rsass-ruby

variables:
  RSASS: /usr/bin/rsass

pages:
  script:
    - rake package
    - mkdir public
    - mv dist public/
    - mv pkg public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
